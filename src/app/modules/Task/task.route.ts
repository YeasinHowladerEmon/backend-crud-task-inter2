import express from "express";


const route = express.Router();

route.get('/', )//get
route.get('/:id', )//id
route.patch('/taskUpdate/:id', )//update
route.delete('/taskDelete/:id', )//delete
route.post('/', )//post

export const taskRoutes = route
